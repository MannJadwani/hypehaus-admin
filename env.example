# Supabase (server-side only)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE=

# JWT secret for admin auth (HS256)
JWT_SECRET=

# Optional: expose URL for client-side reads (if needed)
NEXT_PUBLIC_SUPABASE_URL=

